app:
  # database
  db:
    host: tcp(mysql:3306)
    password: dreamlu
    user: root
  # redis
  redis:
    dB: 0
    addr: redis:6379
    poolSize: 0
    minIdleConns: 0
    #password:
  requestUrl: http://your ip